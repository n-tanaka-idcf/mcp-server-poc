[project]
name = "mcp-server-poc"
version = "0.1.0"
description = "mcp-server-poc"
readme = "README.md"
requires-python = ">=3.14.2"
dependencies = []

[dependency-groups]
dev = ["pytest>=9.0.2", "ruff>=0.14.14"]
