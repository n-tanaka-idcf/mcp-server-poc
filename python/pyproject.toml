[project]
name = "mcp-server-poc"
version = "0.1.0"
description = "mcp-server-poc"
readme = "README.md"
requires-python = ">=3.14.2"
dependencies = [
    "fastmcp>=3.0.0b1",
]

[dependency-groups]
dev = ["pytest>=9.0.2", "ruff>=0.14.14"]
